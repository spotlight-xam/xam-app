import { createNavigationContainerRef } from "@react-navigation/native";

import { MainStackNavigationParamsList } from ".";

export const mainNavigationRef =
  createNavigationContainerRef<MainStackNavigationParamsList>();
